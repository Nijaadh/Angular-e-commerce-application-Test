import { DisabledProductDirective } from './disabled-product.directive';

describe('DisabledProductDirective', () => {
  it('should create an instance', () => {
    const directive = new DisabledProductDirective();
    expect(directive).toBeTruthy();
  });
});
